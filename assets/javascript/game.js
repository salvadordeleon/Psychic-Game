var userNumber = 10; // secret number

//generate the random number function
var randomNumber = math.floor(Math.random() * userNumeber + 1;)
// ask user to pick number between one and ten
var attempts = prompt ("I'm thinking of a number between 1 and " + userNumber);

if (myNum === number) {

alert("Nice! You guessed the secret number!")


}